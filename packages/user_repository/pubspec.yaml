name: user_repository
description: Dart package which manages the user domain.
publish_to: none
environment:
  sdk: ">=3.7.0 <4.0.0"
resolution: workspace
dependencies:
  auth_client: any
  equatable: ^2.0.3
  deep_link_client: any
  package_info_client: any
  rxdart: ^0.28.0
  storage: any
dev_dependencies:
  mocktail: ^1.0.2
  test: ^1.21.4
  very_good_analysis: ^10.1.0
